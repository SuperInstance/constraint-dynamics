[package]
name = "constraint-dynamics"
version = "0.1.0"
edition = "2021"
description = "Physics of constraints — propagation, collision, and emergent structure in agent systems"
license = "MIT"
repository = "https://github.com/SuperInstance/constraint-dynamics"

[dependencies]
serde = { version = "1", features = ["derive"] }
anyhow = "1"
